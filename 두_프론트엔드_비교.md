# 프론트엔드 비교: `expense_frontend` vs `expense_tracker/frontend`

> 작성일: 2026-07-01
> 결론: **두 코드는 일치하지 않음.** 같은 FastAPI 백엔드(포트 5000)와 같은 가계부 도메인을 공유할 뿐,
> `expense_frontend`는 `expense_tracker/frontend`를 최신 스택으로 **처음부터 다시 작성한 별개 코드베이스**.

---

## 1. 개요

| 항목 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| 정체 | 독립 프론트엔드 저장소 (자체 git, 3 커밋) | 풀스택 repo에 내장된 프론트 |
| 위치 | `D:\expense_frontend\` | `D:\expense_tracker\frontend\` |
| 백엔드 | 형제 폴더 `../expense_tracker` (FastAPI) | 상위 폴더 본체 (FastAPI) |
| git | 있음 (단독 저장소) | 없음 (상위가 비-git) |

## 2. 기술 스택

| 항목 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| Next.js | **16.2.7** | 14.2.35 |
| React | **19.2.4** | 18.3.1 |
| Tailwind CSS | **4** (`@tailwindcss/postcss`) | 3.4.15 (autoprefixer/postcss) |
| 서버 상태 | **@tanstack/react-query 5** | 없음 (직접 fetch) |
| 드래그 정렬 | **@dnd-kit** (core/sortable/utilities) | 없음 |
| 차트 | chart.js 4 + react-chartjs-2 5 | chart.js 4 + react-chartjs-2 5 |
| 마크다운 | react-markdown 9 | react-markdown 9 |
| 패키지명 | `expense_frontend` | `gagye-frontend` |
| dev 포트 | `next dev` (기본) | `next dev -p 3000` |

## 3. 디렉터리 구조

| 항목 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| 앱 루트 | `app/` (src 없음) | `src/app/` |
| 훅 | **`hooks/`** (useAuth, useCategories, useSettings) | 없음 |
| 프론트 전용 컴포넌트 | AddTransactionModal, ClientLayout, QueryProvider | AuthShell, charts.tsx, ui.tsx |
| 공통 컴포넌트 | AppShell, ConfirmModal, ToastProvider | AppShell, ConfirmModal, ToastProvider |
| lib | api.ts, types.ts, **utils.ts** | api.ts, types.ts, **format.ts, useTheme.ts** |

## 4. 백엔드 통신 방식

| 항목 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| 호출 주소 | **절대 URL** `API_BASE='http://localhost:5000'` | **상대경로** (`/transaction/...`) |
| 프록시 | 없음 | `next.config.mjs` **rewrites**로 백엔드 프록시 |
| CORS | 필요 (백엔드 `allow_origins`로 허용) | 불필요 (동일 출처) |
| 인증 | 세션 쿠키 (`credentials:'include'`) | 세션 쿠키 (`credentials:'include'`) |
| API 래퍼 | `req<T>()` 함수, named export(`login`, `getMe`...) | `api` 객체 + `ApiError` 클래스 |

## 5. 라우트(페이지) 명명 차이

| 기능 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| 기록 입력 | `/record` (별도 페이지) | `(app)/page.tsx` (루트) |
| 거래 내역 | `/transactions` | `/transactions` |
| 대시보드 | `/dashboard` | `/dashboard` |
| 분석/차트 | `/analytics` | `/chart` |
| 리포트 | `/report` | `/report` |
| 예산 | `/budget` | `/budget` |
| 카테고리 | `/categories` | `/category` |
| 구독/정기결제 | `/subscriptions` | `/subscription` |
| 검색 | `/search` | `/search` |
| AI 조언 | `/ai-advisor` | `/advisor` |
| 인증 | `/login`, `/register` | `/login`, `/register` |

> 명명 패턴: frontend는 복수·서술형(categories/subscriptions/ai-advisor/analytics),
> tracker는 단수형(category/subscription/advisor/chart).

## 6. 파일 규모(라인 수) — 대응 파일이 전부 다름

| 파일 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| 기록 페이지 | 244 (`record`) | 334 (`(app)/page`) |
| dashboard | 250 | 313 |
| transactions | 229 | 317 |
| AI 조언 | 227 (`ai-advisor`) | 333 (`advisor`) |
| 카테고리 | 201 (`categories`) | 275 (`category`) |
| subscription(s) | 152 | 181 |
| search | 140 | 174 |
| budget | 106 | 143 |
| report | 90 | 148 |
| login | 119 | 98 |
| register | 85 | 106 |
| globals.css | 204 | 146 |
| lib/api.ts | 158 | 67 |
| lib/types.ts | 80 | 62 |
| AppShell | 291 | 315 |

## 7. 구현 차이 (단순 포맷이 아닌 설계 차이)

| 측면 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| 따옴표 | 작은따옴표 `'` | 큰따옴표 `"` |
| 인증 처리 | `useQueryClient` + `invalidateQueries(['me'])` | `useToast` + `AuthShell`/`AuthField` |
| 폼 state | `form` 객체 하나 | `userId`/`password` 분리 |
| 에러 표시 | 로컬 `error` state | `toast` |
| 타입 정의 | `CategoryChart`, `CategorySummary`, `PaymentSummary` 인터페이스 | `TxnType` 별칭, `MonthlySummaryRow` |
| 테마 | (globals.css 기반) | `useTheme.ts` 훅 |

## 8. API 계약 차이 (엔드포인트)

공통(둘 다 사용): `/auth/login`, `/auth/me`, `/auth/register`, `/ai/analyze`, `/ai/chat`,
`/transaction/add`, `/transaction/categories`, `/transaction/category-chart`,
`/transaction/category/add`, `/transaction/category/reorder`, `/transaction/data`,
`/transaction/payment-summary`, `/transaction/process-recurring`, `/transaction/recurring/add`,
`/transaction/recurring/list`, `/transaction/reset`, `/transaction/settings/payment-methods`,
`/transaction/settings/user-labels`, `/transaction/summary`, `/transaction/yearly-summary`

| 엔드포인트 | expense_frontend | expense_tracker/frontend |
|---|---|---|
| `/transaction/category-summary` | ✅ 사용 | ❌ |
| `/auth/logout` | ❌ | ✅ |
| `/transaction/list` | ❌ | ✅ |
| `/transaction/export` | ❌ | ✅ |
| `/transaction/recent` | ❌ | ✅ (`?limit=8`, `?limit=20`) |
| `/transactions` | ❌ | ✅ |

> 거래 목록 조회를 frontend는 `/transaction/data`로 통일, tracker는 `/list`·`/recent`·`/transactions`를 추가 사용.

## 9. 특이사항

- `expense_frontend/AGENTS.md`: "이 Next.js는 학습 데이터와 다른 breaking-change 버전이니,
  코드 작성 전 `node_modules/next/dist/docs/`를 읽으라"고 명시 (Next 16 신규 규약 주의).
- `expense_tracker/CLAUDE.md`: 코딩 규칙(단순성·외과적 변경·UTF-8), 백엔드 아키텍처(pymysql + PooledDB, raw SQL),
  rewrites 프록시 구조를 상세 문서화.

---

## 최종 정리
- **소스 일치 여부: 불일치.** 같은 파일을 복사·수정한 관계가 아니라, 동일 도메인을 **독립적으로 재구현**한 두 코드베이스.
- 공유점: 가계부 도메인 + 동일 FastAPI 백엔드(대부분의 엔드포인트).
- 차이점: 스택 세대(Next14/React18 → Next16/React19), 상태관리(직접 fetch → React Query),
  통신 방식(rewrites 프록시 → 절대 URL+CORS), 라우트 명명, 파일 구조, 구현 디테일, 일부 API 계약.
