# 배포 재현성을 위해 버전을 고정한다. 업그레이드 시 로컬에서 검증 후 이 값을 올린다.

# ── 웹 프레임워크 ──
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
python-dotenv==1.2.2
itsdangerous==2.2.0     # Starlette SessionMiddleware 쿠키 서명
werkzeug==3.1.8         # password hashing (generate_password_hash / check_password_hash)

# ── 데이터베이스 ──
pymysql==1.1.2
cryptography==47.0.0    # pymysql MySQL8 caching_sha2_password 인증 + 자격증명 암호화
dbutils==3.1.2

# ── 소셜 로그인 (OAuth 토큰 교환·userinfo 조회) ──
# anthropic/openai 의 전이 의존으로도 깔리지만, social_auth 가 직접 쓰므로 명시 고정한다 —
# AI SDK 를 빼거나 올릴 때 인증이 조용히 깨지지 않게.
httpx==0.28.1

# ── AI (BYOK: 사용자별 키로 Claude·GPT·Gemini) ──
anthropic==0.97.0
openai==2.46.0
google-genai==2.12.1
