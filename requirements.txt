# ── 웹 프레임워크 ──
fastapi
uvicorn[standard]
python-multipart
python-dotenv
itsdangerous     # Starlette SessionMiddleware 쿠키 서명
werkzeug         # password hashing (generate_password_hash / check_password_hash)

# ── 데이터베이스 ──
pymysql
cryptography     # pymysql MySQL8 caching_sha2_password 인증
dbutils

# ── AI (Claude) ──
anthropic
